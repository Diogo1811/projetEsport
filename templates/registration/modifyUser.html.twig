{# {% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

    <h1>Register</h1>

    {{ form_errors(modifyUserForm) }}

    {{ form_start(modifyUserForm) }}
        {{ form_row(modifyUserForm.username) }}
        {{ form_row(modifyUserForm.email) }}
        {{ form_row(modifyUserForm.oldPassword) }}
        <div class="col-2 formRowPassword">
            {{ form_row(modifyUserForm.plainPassword, {
                label: 'Password'
            }) }}
        </div>
        {{ form_row(modifyUserForm.profilePicture) }}


    {{ form_end(modifyUserForm) }}
{% endblock %} #}